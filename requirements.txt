# Web Framework & Servidor
fastapi>=0.110.0
uvicorn[standard]>=0.28.0
python-dotenv>=1.0.0
pydantic>=2.6.0
pydantic-settings>=2.2.0
starlette

# Banco de Dados & Async
# COMENTÁRIO: sqlalchemy é usado APENAS pelo alembic (versionamento do schema).
# Todas as consultas do projeto continuam em psycopg puro — ver specs/004-alembic-migrations/.
sqlalchemy>=2.0.0
alembic>=1.13
psycopg[binary]>=3.1.0
psycopg-pool

# LangChain, LangGraph & IA
langchain>=0.1.0
langchain-community
langchain-core
langchain-openai
langchain-ollama
langchain-chroma
langchain-huggingface
langgraph
langgraph-checkpoint
langgraph-checkpoint-postgres
langsmith

# Vetores, Embeddings & ML
# COMENTÁRIO: torch é instalado separadamente no Dockerfile (build CPU-only),
# pois a VM de produção é ARM64 sem GPU e o índice padrão do PyPI traz dependências CUDA desnecessárias
openai
sentence-transformers
transformers

# Utilitários & Arquivos
pandas
openpyxl
pypdf
requests
httpx
tiktoken

langchain-postgres
psycopg[binary]
pgvector

#Google Calendar
google-api-python-client
google-auth
google-auth-httplib2

PyJWT
slowapi

# Fila de retry de uso de token (EDI-63)
redis
# EmailStr (validação de notification_emails/global_notification_recipients, EDI-63)
email-validator

# Testes
pytest>=8.0.0